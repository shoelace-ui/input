/**
 * Module dependencies
 */

@require 'shoelace-ui-duration'
@require 'shoelace-ui-font-size'
@require 'shoelace-ui-line-height'
@require 'shoelace-ui-padding'
@require 'shoelace-ui-gray'

/**
 * Define `input` variables and placeholder
 */

input--color          ?= gray--darkest
input--font-size-base ?= font-size--base

input--bg          ?= white
input--bg-disabled ?= gray--lighter

input--border-width       ?= 1px
input--border-color       ?= desaturate(lighten(input--color, 70%), 90%)
input--border-color-focus ?= desaturate(lighten(input--color, 60%), 90%)
input--border-radius      ?= 2px

input--box-shadow       ?= inset 0 1px 1px rgba(0,0,0, .055)
input--box-shadow-focus ?= inset 0 1px 1px rgba(0,0,0, .055), 0 0 8px input--border-color-focus

input--height-base  ?= (line-height--md-computed + ((padding--md + input--border-width) * 2))
input--height-large ?= (line-height--lg-computed + ((padding--lg + input--border-width) * 2))
input--height-small ?= (line-height--sm-computed + ((padding--sm + input--border-width) * 2))

input--transition-property   ?= border-color box-shadow
input--transition-duration   ?= duration--base
input--transition-timing     ?= ease-in-out

input--padding-y ?= padding--md
input--padding-x ?= padding--lg

input--apply                ?= {}
input--apply.text            = true
input--apply.password        = false
input--apply.datetime        = false
input--apply.datetime-local  = false
input--apply.date            = false
input--apply.month           = false
input--apply.time            = false
input--apply.week            = false
input--apply.number          = false
input--apply.email           = false
input--apply.url             = false
input--apply.search          = false
input--apply.tel             = false
input--apply.color           = false
